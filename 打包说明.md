# 产品经理工作台源码包

包含 AionUi（桌面端/前端）与 AionCore（Rust 后端）的当前工作区源码、资源、测试、文档、许可证和锁文件，包含未提交修改及新增文件。

打包范围：两个仓库 Git 跟踪的现存文件，以及未被 .gitignore 排除的新增文件。使用工作区当前内容，不局限于最后一次提交。

排除：Git 历史及 .git 元数据、node_modules、Rust target、out 等构建产物、被仓库忽略的本地配置及工作记录、外层 .tools 临时工具依赖。应用的用户数据目录、聊天记录、登录信息不属于此源码包。

FILES.sha256 记录原始项目文件校验值。保留两个项目原有目录关系、构建配置、文档与许可证。开发及构建说明见 AionUi/readme.md、AionUi/package.json、AionCore/ARCHITECTURE.zh-CN.md 和各自 justfile。

上传源码仓库时，请先解压，再提交 product-manager-workbench 目录里的内容。两个子目录不包含嵌套 .git，可放入同一个仓库。此压缩包是源码快照，不是可直接运行的安装包，构建前需按项目说明安装依赖。
